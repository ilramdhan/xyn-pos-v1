apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: xyn-dev

resources:
  - ../../../base/web

images:
  - name: ghcr.io/xyn-pos/xyn-pos-web
    newTag: dev

patches:
  - patch: |-
      - op: replace
        path: /data/NEXT_PUBLIC_API_URL
        value: https://api.dev.xyn-pos.dev
      - op: replace
        path: /data/NEXT_PUBLIC_APP_ENV
        value: development
    target:
      kind: ConfigMap
      name: web-config
  - patch: |-
      - op: replace
        path: /spec/rules/0/host
        value: app.dev.xyn-pos.dev
      - op: replace
        path: /spec/tls/0/hosts/0
        value: app.dev.xyn-pos.dev
      - op: replace
        path: /spec/tls/0/secretName
        value: web-dev-tls
    target:
      kind: Ingress
      name: web
